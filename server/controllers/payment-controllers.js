/**
 * @file Manages all payment functionality.
 * @version 1.0.0
 */
const db = require('../db');
const pool = db.pool;

async function addPayment(amount, table_number, card_number, card_expiry, card_){}